# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (contain secrets — never commit)
.env
.env.*
!.env.example

# Firebase service account (server-side, contém chave privada — nunca commitar)
firebase-service-account.json

# Supabase CLI local state — contém ref do projeto e connection string (segredo)
supabase/.temp/

# Arquivos temporários do runtime do Claude Code
.claude/scheduled_tasks.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# Artefatos locais do Playwright MCP (snapshots/screenshots de teste)
.playwright-mcp/

# Cópia aninhada do projeto (não versionar)
rasta-wave-flow-main/

# Capturas geradas para o Play Console (uso pontual, ficam no dashboard da loja)
screenshots/

# Saída do playwright test (traces, reports, screenshots de falhas)
test-results/
playwright-report/
